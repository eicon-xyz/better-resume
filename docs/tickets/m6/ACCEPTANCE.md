# M6 验收：分布式模式 + 压测 + skills 知识库

- 日期：2026-09-14（CST）；分支 `m6/distributed`
- 环境：WSL2 Ubuntu 22.04.5 / Docker Desktop 29.6.2 + compose 5.3.1 / 20 vCPU / 7 GiB
- 原话验收（§12.4）：**"kill 实例恢复面试会话不丢状态"** → §1 是它的脚本化输出。

---

## 1. §12.4 硬验收：kill 实例端到端（T8）

命令：`bash scripts/kill_instance_drill.sh`（起栈 → nginx + 2×api + worker + fake-llm → 走完半程面试 →
`docker kill` 正在服务该会话的实例 → 经 nginx 由另一个实例接管 → 继续答题/restore/finish/report）

```
login: 200 instance=7ffc9ff32570
scenes -> smoke-fake: chat, question_extraction, answer_evaluation, follow_up, report_summary
questions: 3 generated by instance=7ffc9ff32570
answered 1: score=55.0 instance=7ffc9ff32570
  state before kill: {"status": "follow_up", "current_question_no": "1-F1", "answered": 1, "total_questions": 3, "last_score": 55.0, "last_question_no": "1"}
killed instance 7ffc9ff32570
nginx now routes to d73b8b245ec0 (first healthy response after 1106 ms in 2 attempts of 1s, kill -> recovery 1304 ms)
replayed request on d73b8b245ec0: replayed=True score=55.0
  state after kill:  {"status": "follow_up", "current_question_no": "1-F1", "answered": 1, "total_questions": 3, "last_score": 55.0, "last_question_no": "1"}
  redis keys mid-flow: {"locks": 0, "hot_state": 1, "singleflight": 0, "jobs": 4}
finish: status=finished overall=55.0 turns=9 summary_pending=True instance=d73b8b245ec0
report summary from the worker: '整体结构清晰，深度尚可；建议用数据说明取舍与结果。'
frozen report: {"overall": 55.0, "dimensions": {"accuracy": 55.0, "depth": 55.0, "coverage": 76.0, "completeness": 100.0}, "turns": [["1","main",55.0],["1-F1","follow_up",55.0],["1-F2","follow_up",55.0],["2","main",55.0],["2-F1","follow_up",55.0],["2-F2","follow_up",55.0],["3","main",55.0],["3-F1","follow_up",55.0],["3-F2","follow_up",55.0]], "suggestions": ["反复缺失的要点：失败复盘、量化结果", "回答偏结论、缺过程：补充当时的取舍、失败尝试与量化结果"]}
redis keys at the end: {"locks": 0, "hot_state": 0, "singleflight": 0, "jobs": 6}
redis key sample: ["br:jobs:dedupe:report.summary:report:ad6389b5-…", "br:jobs:status:v8H1Tk9Gn8ZpIx2Z", "br:jobs", "br:jobs:dead", "br:jobs:health"]

== drill summary ==
instances: served=7ffc9ff32570 survivor=d73b8b245ec0 kill->recovery=1304 ms
PASS: 会话在实例被 kill 后由另一实例接管，状态、分数与冻结报告全部一致
```

断言逐条对应：

| 验收要求 | 实测 | 证据行 |
| --- | --- | --- |
| 请求落到**另一个**实例 | `served=6f39c80a0fc1` → `survivor=f9ba0be79fc3`（`X-Instance-Id`） | `nginx now routes to …` |
| kill → 恢复耗时 | **1304 ms**（第 1 次 1s 探针被死上游拖住，第 2 次成功；早期 5s 探针报过 237 ms/5448 ms，那是**探针粒度**而非 nginx 差异，见 §8） | `kill -> recovery` |
| 题号 / 已答数 / 分数 / 追问计数不丢 | kill 前后 `state` 字典**逐字段相等**（题号 1-F1、answered 1、score 55.0） | `state before/after kill` |
| 重试不重复计费 | 同 `request_id` 重放 → `replayed=True`，分数不变 | `replayed request on …` |
| 报告数字 = 冻结值 | overall 55.0、四维、9 个 turn 的分数全部来自 kill 前的作答 | `frozen report` |
| 排队任务在别的进程完成 | `summary_pending=True` → worker 写出总结 | `report summary from the worker` |
| 分布式组件真的在 Redis 里 | 半程 `hot_state=1`，结束时锁已释放、热层被写路径失效（0） | `redis keys mid-flow` / `at the end` + key sample |

> `locks=0`/`singleflight=0` 是**正常**值：锁在答题结束即释放，单飞结果只在调用进行中与回放窗口内存在。
> drill 同时打印它统计的原始 key 样本，便于核对（见 PROBLEMS P14）。

## 2. 分布式单飞 / 锁的机制证据（T1/T2/T3）

| 机制 | 断言 | 位置 |
| --- | --- | --- |
| 题级锁互斥 | 两个并发请求只有一个拿到锁；另一个等待后接管 | `tests/test_distributed_lock.py`（10 例，含续租/过期/释放者校验） |
| 跨实例单飞 | 两个 `RedisFlight` 实例并发同 key → 上游只被调用 **1** 次；结果按 replay TTL 回放；失败可缓存 | `tests/test_distributed_flight.py`（10 例） |
| fencing | 租约过期的 owner 回来写结果会被拒（fencing token） | 同上 `test_stale_owner_is_fenced_out` |
| 热层 + 失效 | 读命中热层报 `source="hot"`；写路径失效后回到 `derived` | `tests/test_hot_state.py`（8 例）+ 6 个回归用例 |
| 任务队列 | 去重、重试、死信、XPENDING+XCLAIM 崩溃接管 | `tests/test_job_queue.py`（7 例） |
| worker 韧性 | Redis 抖动后循环继续；干净停止删心跳 | `tests/test_worker_loop.py`（4 例） |
| 两实例只调一次上游（HTTP 层） | 场景绑定 + 单飞：`tests/test_distributed_flight.py::test_two_instances_call_the_vendor_once` | 同上 |

## 3. 容量报告摘要（T6）

完整报告：`docs/perf/M6-capacity.md`（含环境表、三组场景、瓶颈分析、8 条不确定项）。

| 场景（假供应商，2×api 经 nginx） | count / ok / rps | p50 / p95 / p99 (ms) | 429 | 备注 |
| --- | --- | --- | --- | --- |
| mixed c=20 d=20 | 935 / 684 / 41.7 | 17.3 / 2464 / 3856 | 18 | `http_409: 233`（并发答题的状态机拒答） |
| chat-sse c=20 d=20 | 174 / 174 / 7.8 | 2444 / 2721 / 2847 | 0 | 首帧 2030ms 中 2000ms 是假供应商固定延迟；**我们的增量约 30ms** |
| answer-submit c=10 d=20 | 11701 / 1 / 584 | 10.7 / 46.8 / 92.8 | 11350 | 默认限流 20 req/s：这组数字量的是**限流器**，不是答题容量 |

**没有真模型数字**（§2.4 为空，原因如实写在报告里）；真模型高并发、多可用区、生产网络仍是未验证项。

## 4. skills 知识库（T7）

```
$ cd apps/api && uv run python scripts/extract_api_index.py --check
API index is up to date          # exit=0
$ uv run pytest tests/test_skills_index.py -q   →  tests 39 failures 0 errors 0
```

| 交付物 | 内容 |
| --- | --- |
| `skills/repo-map/SKILL.md` | 15 行路由表（11 个模块 + 前端 + 两跳示例）：改 X → 先看哪几个文件/不变量/测试 |
| `skills/modules/*/SKILL.md` | 10 份（30–37 行 ≤80）：职责 / 对外接口 / 不变量 / 已知陷阱（带 `M<P>` 编号）/ 测试地图 / 变更配方 |
| `skills/api-index/generated-api-index.md` | 脚本生成：24 个 REST 端点 + 1 个 WS + 8 条前端路由（含权限、幂等语义、前端消费点） |
| `apps/api/scripts/extract_api_index.py` | 生成器，`--check` 漂移检查；CI 新增一步（`ci.yml` backend job） |
| `apps/api/tests/test_skills_index.py` | 39 例：端点集合 == openapi paths、漂移可检可修、小节/行数、repo-map 覆盖、PROBLEMS 交叉引用 |

人工两跳抽查（票据要求的 3 个问题，记录过程）：

1. **"限流 429 之后前端为什么清空输入？"** → repo-map 的 http 行 → `skills/modules/http/SKILL.md` 陷阱 →
   M3 P10（输入框与流的耦合）。**2 跳**。
2. **"换供应商要改哪些文件？"** → repo-map 的 llm_gateway 行 → `llm_gateway/{scenes,binding_store,resolver}.py`
   + `tests/test_scene_routing.py`（业务代码不动）。**2 跳**。
3. **"kill 掉一个 api 实例为什么还能继续答题？"** → repo-map 的 interview_engine 行（热层不变量）+
   ai_resilience 行（锁/单飞在 Redis）→ 对应 `tests/test_hot_state.py` 与 drill。**2 跳**。

**路径偏差**：票据写 `scripts/extract_api_index.py`，仓库里实际是 `apps/api/scripts/extract_api_index.py`
（与 `export_openapi.py` 同层，用 api 的 uv 环境运行）；已写进 `skills/README.md` 与脚本 docstring。

## 5. 本地 CI 全矩阵

| # | 检查 | 命令 | 结果 |
| --- | --- | --- | --- |
| 1 | 格式 | `uv run ruff format --check .` | 216 files already formatted |
| 2 | 静态检查 | `uv run ruff check .` | All checks passed |
| 3 | 后端测试（带测试库） | `BR_DATABASE_URL=…:5433/… uv run pytest -q`（--junitxml 计数） | **648 passed / 0 failed / 0 error / 0 skipped**（M5 528 → 648） |
| 3b | 后端测试（干净环境，无 `BR_*`） | `env -u BR_DATABASE_URL -u BR_REDIS_URL uv run pytest -q` | **648 tests / 0 failed / 132 skipped**（需要库的用例显式 skip，不再抛 asyncpg traceback） |
| 4 | 迁移一致性 | `uv run alembic check` | No new upgrade operations detected |
| 5 | OpenAPI 漂移 | `uv run python scripts/export_openapi.py --check` | up to date |
| 6 | skills 索引漂移 | `uv run python scripts/extract_api_index.py --check` | up to date |
| 7 | 前端 lint | `pnpm -C apps/web lint` | 通过 |
| 8 | 前端类型 | `pnpm -C apps/web typecheck` | 通过 |
| 9 | 前端测试 | `pnpm -C apps/web test --run` | 23 files / **158 passed**（M6 无前端改动，与 M5 持平） |
| 10 | compose 清单 | `docker compose config` + `tests/test_deploy_manifest.py`（14 例） | 通过 |
| 11 | 部署冒烟 | `bash scripts/compose_smoke.sh` | **ALL CHECKS PASSED**（T5-EVIDENCE.md） |
| 12 | kill 实例 drill | `bash scripts/kill_instance_drill.sh` | **PASS**（§1） |
| 13 | 容量脚本自测 | `uv run pytest tests/test_load_test_script.py` | 6 passed（MockTransport，不测性能） |
| 14 | 演练前置语义 | `uv run pytest tests/test_drill_prereqs.py` | 3 passed（无 docker 时 exit 2） |

CI 工作流（`.github/workflows/ci.yml`）在原有 dual-job 上新增第 6 项；本地逐条等价执行过。

## 6. 未验证项（诚实清单）

1. **真模型**：本轮所有 AI 调用都打在确定性假供应商上（成本/稳定性考虑）；真实 DeepSeek/星云的高并发、
   限流与长尾延迟**未测**（容量报告 §2.4 为空）。
2. **真机凭据相关**：M4 讯飞 AST 真机、M5 星云真机仍未验证（本机无凭据）——与 M4/M5 的 ACCEPTANCE 一致。
3. **真实浏览器手测**：M6 未改动前端，但"麦克风采集/播放"的人工 4 步清单仍未在真浏览器执行过。
4. **多可用区 / Redis 集群 / 网络分区**：只有单机 Redis；"分布式"覆盖的是单机多进程。
5. **生产网络与 LB**：nginx 在 compose 内网，没有真实 LB 健康检查抖动与跨机 RTT。
6. **长时间稳定性**：压测窗口 15–20s，未做 >10 分钟浸泡（内存/连接泄漏）。
7. **默认分布式开关**：`BR_FLIGHT_BACKEND`/`BR_LOCK_BACKEND`/`BR_HOT_STATE_BACKEND` 的默认值仍是
   `memory`（单进程开发友好）；compose 里全部显式设为 `redis`，drill 就是在 `redis` 下跑的。

## 7. 与提案的偏差清单

| 偏差 | 原因 | 影响 |
| --- | --- | --- |
| `extract_api_index.py` 落在 `apps/api/scripts/`（票据写 `scripts/`） | 与 `export_openapi.py` 同层、共用 api 的 uv 环境（脚本要读 `apps/api/openapi.json`） | 仅路径；CI 与文档已同步 |
| T5 的 `apps/web/Dockerfile` 只做静态站点，反向代理由同一个容器的 nginx（挂载 `deploy/nginx.conf`）承担 | 一个入口容器同时提供 SPA 与 `/api` 反代，避免再拆一个 nginx 服务 | 与票据的"nginx 服务"等价，服务名仍叫 `nginx` |
| 新增 `migrate` 一次性服务与共享 `uploads` 卷 | 起栈时没有迁移会 500（P10）；简历文件必须每个副本可见（P11） | 服务数 6（含 migrate，退出即完成）；票据未禁止 |
| 压测 `answer-submit` 场景实测被默认限流削峰（429 >96%） | 脚本无思考时间 + 默认 20 req/s 桶 | 该场景数字标注为"限流下的表现"，不是答题容量（容量报告 §2.3） |
| 前端测试数 158（票据写"≥165，若有前端改动"） | M6 没有前端改动 | 如实记录，无新增前端用例 |
| 真实 kill→恢复耗时两次分别 237 ms / 5448 ms | nginx 何时发现死上游取决于请求时序（`fail_timeout`） | 两个数字都写进 §1，不取最好看的那个 |

## 8. 人工验收反馈后的修正（2026-09-14，P15）

用户在干净 shell 里复跑时暴露了 5 个失败，全部与功能无关、全部已修（`PROBLEMS.md` P15）：

| 现象 | 根因 | 修法 | 证据 |
| --- | --- | --- | --- |
| `test_client_survives_malformed_no_proxy` 断言失败（`trust_env=True`） | 用例依赖环境里的代理变量顺序：urllib 只取第一个 `*_proxy` 后缀键，用户 shell 的小写 `no_proxy` 先命中，畸形值被遮蔽 | 用例先删掉所有 `*_proxy` 再设 `NO_PROXY=[::1]`；另加确定性孪生用例（monkeypatch `httpx.AsyncClient` 抛 `InvalidURL`） | `env no_proxy=localhost NO_PROXY=localhost uv run pytest tests/llm_gateway/test_openai_compat.py` → 修前 1 failed / 修后 0 failed |
| 4 个 `asyncpg ConnectionRefused 5432` | 需要数据库的用例没声明 `migrated_database`，拿不到 skip 保护 | `test_ratelimit_http.py::tight_client` + `test_resilience_concurrency.py` 两个自建 client 的用例补 fixture | 干净环境：failures 4 → 0，skipped 124 → 132 |
| `需要 uv`（drill 在无 `~/.local/bin` 的 shell 直接退出） | 脚本只信 PATH | 两个脚本自己找 `$HOME/.local/bin/uv`，找不到才给可复制的 `export PATH` 提示 | `env -i PATH=/usr/bin:/bin bash scripts/kill_instance_drill.sh` → PASS |
| `kill -> recovery 5.4s` 读起来像 nginx 慢 | 探针用 5s 超时，第一个被死上游拖住的请求把数字撑到了超时上界 | 探针改 1s 窗口并打印尝试次数 | 新输出：`first healthy response after 1106 ms in 2 attempts of 1s, kill -> recovery 1304 ms` |
| `pnpm -C apps/web` 报 `ENOENT …/apps/apps` | 在 `apps/api` 下 `cd ..` 到了 `apps` | README 写明从仓库根跑（或 `-C ../web`） | `pnpm -C apps/web test --run` → 23 files / 158 passed |

修正后重新执行：后端 **648 passed**（带库）/ **132 skipped、0 failed**（干净环境）、前端 **158 passed**、
`bash scripts/compose_smoke.sh`（最小 PATH）**ALL CHECKS PASSED**、`bash scripts/kill_instance_drill.sh`（最小 PATH）**PASS**。
