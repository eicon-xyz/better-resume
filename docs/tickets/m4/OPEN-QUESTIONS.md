# M4 待确认项（决策请拍板，我不自行决议）

| 编号 | 一句话 | 我的提议 |
| --- | --- | --- |
| Q1 | 讯飞凭据策略 | 现在写完整 AST adapter + 假 adapter 演示，真机验证单列 T10 等你给凭据（**绝不假数据冒充真机**） |
| Q2 | TTS 供应商 | edge-tts（在线，`speech.platform.bing.com:443` 本机可达）+ 磁盘缓存 + 失败降级；不引第三家 |
| Q3 | 前端采集形态 | AudioWorklet 采 16k 单声道 PCM（40ms/1280B 切片）+ 一次性 ticket 的 WS；不引录音库 |
| Q4 | 句池移植深度 | 完整移植（apd/rpl/无 pgs 演化/尾缀合并/final），照搬原反射单测语义 |
| Q5 | 消费方范围 | 一个 hook + 一份 store，同时接面试答案框与对话输入框（D15）；播放器只做"朗读题目" |

---

## Q1 讯飞凭据

本机没有 `appId / accessKeyId / accessKeySecret`（我不会去猜或用假数据）。

- **A（提议）**：T2 按 §4.4 实现完整协议（签名/推流/解析/重连），用本地假 WS 服务端做契约测试；
  T9 用 `ScriptedTranscriptionChannel` 跑通端到端；T10 留真机验证，**等你的凭据**。
  验收文档里"语音答题全流程"写明是契约级 + 待真机确认。
- B：先只做假 adapter 与前端，讯飞协议代码等有凭据再写（省 1.5 会话，但 M4 的亮点③就只剩前端）。
- C：你现在就给凭据，我直接做真机（+0.5 会话，T10 与 T2 合并验收）。

## Q2 TTS

- **A（提议）**：edge-tts（`zh-CN-XiaoxiaoNeural` 默认），`data/tts/{sha256}.mp3` 内容寻址缓存，
  并发同文本复用 M3 单飞；供应商失败返回 503 + 前端"暂不可用"，不静默播空音频。
- B：假 TTS（返回预置 mp3），等你说能出网/给出供应商再换。
- C：讯飞长文本 TTS（与 Q1 绑定，需要凭据，且短句场景要轮询，成本高）。

## Q3 前端采集

- **A（提议）**：AudioWorklet → 16k 单声道 Int16 → 40ms(1280B) 切片，WS 走一次性 ticket；
  这套与讯飞要求逐字对齐，也是原项目 `audioTranscription` 的形态。
- B：MediaRecorder 录整段，停止后一次性上传做离线转写（实现量小，但**不是"实时上屏"**，
  且要后端再引一个离线 ASR，与 D06 冲突）。
- C：只做后端 WS + 脚本客户端，前端等 M5（验收会缺"语音答题"这一半）。

## Q4 句池移植深度

- **A（提议）**：完整移植（含无 pgs 的重叠度演化与尾缀合并）——这是 D16 亮点③ 的本体，
  也是旧简历"独家亮点"里唯一还没落地的一条。
- B：只做 apd + final（省 1 会话，但 rpl/演化的坑与经验就丢了，面试也讲不出细节）。

## Q5 消费方范围

- **A（提议）**：一个 `useTranscription` + 一份 store，面试答案框与对话输入框共用（D15）；
  播放器只做"朗读题目"（不自动播、不连播）。
- B：先只接面试答案框，对话页留 M5（少 0.5 会话，但 D15 的"一个通道两个消费方"要等下一个里程碑才完整）。

