# P4 阶段问题台账（P35）

| 编号 | 一句话 | 修法 | 测试/证据 |
| --- | --- | --- | --- |
| P35 | **三个演练脚本先 seed 后 migrate**：`compose_smoke.sh:55` / `kill_instance_drill.sh:52` / `fault_injection_drill.sh:62` 都是 `up -d --wait postgres redis` → 立刻 `INSERT INTO ai_models`，而 `migrate` 服务要等到后面的 `up` 才跑。本机 `better-resume_pgdata` 是**热卷**（表早就在，实测 `ai_models` 6 行、alembic `9f2c1a7b5d31`）→ 一直绿；CI 是**冷卷** → `relation "ai_models" does not exist`。后果：**P2 建的自动回归网（nightly / weekly）从第一次运行起就没绿过** —— nightly 连红 5 晚（09-18 ~ 09-22，每次 24–31 秒即挂）、weekly-full 红 1 次（09-20）。`compose.yaml:72` 的注释其实早写过这个坑（T5 smoke 踩过），只是演练脚本没照做 | 三个脚本在 seed 前显式跑一次性迁移作业 `docker compose run --rm --build migrate`（幂等；后面 `up` 再跑一次无害）。顺手补 `compose_smoke.sh` **缺失的 docker 守卫**：另两个脚本早有（`command -v docker` → exit 2 + 说明），只有它没有，无 docker 时会死在 `docker compose config` 的裸 127，不算"明确失败"（P2-T6 的规矩） | **红**：冷卷复跑 `ERROR: relation "ai_models" does not exist`（`var/evidence/p35/01-cold-red.log`）+ `test_drills_migrate_the_schema_before_seeding_the_smoke_model` 3 例全红（`assert None is not None`）；**绿**：`tests/test_drill_prereqs.py` **10 passed**、冷卷 `compose_smoke.sh` 全 PASS（`var/evidence/p35/02-cold-green.log`）；**CI**：nightly `workflow_dispatch` run |

| P36 | **`kill_instance_drill.py` 的作答循环撞限流后以 `KeyError` 崩溃**：演练按假上游的响应速度连发 8 次作答（默认 `--max-answers 8`），而 answer 桶是 **2 rps + 2x 突发**（P1-B 按真实单用户 ~0.2 rps 标定，M6 §2.7）→ 第 5 次起 429；循环直接 `.json()["next_action"]`，于是**一个正常的限流被报成服务端 bug**（`KeyError: 'next_action'`）。P35 修好冷库后演练第一次真正跑到这一段才暴露 | 演练新增 `post_answer()`：429 时按 `Retry-After` 退避后用**同一个 `request_id`** 重试（作答接口幂等），其余 ≥400 一律带响应体抛 `RuntimeError`——不再用 KeyError 冒充结论；另加纯函数 `retry_after_seconds()` 解析头（缺失/非数字回退 1s）。**产品行为不改**：限流是 M3-T6 的正确行为，错的是探针 | 红：冷卷演练实跑 `KeyError: 'next_action'` + api 日志 `rate_limited bucket=answer retry_after=1` → 429（`var/evidence/p35/03-cold-kill-drill.log`）；绿：`test_post_answer_honours_retry_after_then_succeeds` / `test_post_answer_raises_with_the_body_instead_of_a_keyerror` / `test_retry_after_seconds_parses_the_header_with_a_fallback`（5 例）→ `test_drill_prereqs.py` **17 passed**；冷卷复跑见 ACCEPTANCE |

| P37 | **`fault_probe.py` 的 worker-crash 实验紧跟在 failover 之后，被一个 503 打死**：故障 4 恢复原主节点后，下个实验（worker-crash）立刻登录，拿到 `503 Service Unavailable`（P17 语义：依赖还没就绪），脚本 `login.raise_for_status()` 直接抛栈、整轮演练 exit 1。**同一个坑 soak 早就踩过并修过**（源码注释：「observed after a failover: retry the login instead of aborting an hour-long soak on one 503」），但那段重试逻辑是**内联在 soak 里的**，worker-crash 用不上 | 把 soak 的内联重试抽成共享的 `login_with_retry()`（任何 ≥400 重试 5 次、每次 1s，最后仍失败才 `raise_for_status()`），`soak` 与 `worker_crash` 都调它——重复代码消失，行为对 soak 逐字不变 | 红：冷卷演练 `httpx.HTTPStatusError: Server error '503 Service Unavailable' for url '.../api/v1/auth/session'`（`var/evidence/p35/04-cold-fault-drill.log`，故障 1–4 全 PASS、5 崩）；绿：`test_login_with_retry_survives_a_stale_connection`（503,503,200 → 200）与 `test_login_with_retry_still_raises_when_the_dependency_stays_down`（503×3 → 抛且只试 3 次）→ `test_fault_probe.py` **9 passed**；冷卷复跑见 ACCEPTANCE |

| P38 | **worker-crash 实验在 failover 之后立刻开跑，撞上"依赖还没就绪"的 503 窗口**：故障 4 恢复原主节点后，场景/注册表缓存仍持续回 503（P17 语义，实测两副本在恢复后 >24s 仍 503；`/healthz` 全程 200，因为它不碰 Redis），而 worker-crash 的登录+绑定场景在启动时就要用这条路径 → P37 只重试了登录，紧接着 `PUT /api/v1/scenes/chat` 又 503。**实验测的是"崩溃消费者的任务能否被接管"，不是"冷启动能不能扛"** | `fault_probe` 新增 `wait_until_the_stack_is_usable()`：在 worker-crash 真正开始前，用 `asyncio.timeout(--ready-timeout，默认 120s)` 反复「登录 + 绑定 5 个场景」，只有 503 才重试（其它错误立刻抛），超时抛 `RuntimeError("the stack still answered 503 after Ns")`——**等不到就响亮失败，不静默通过** | 红：`PUT /api/v1/scenes/chat → 503`（`var/evidence/p35/04-cold-fault-drill.log`，故障 1–4 PASS、5 崩）；绿：`test_wait_until_the_stack_is_usable_rides_out_a_settling_cache`（503,503 → 通过）与 `..._still_fails_when_it_never_recovers`（永不恢复 → RuntimeError 且消息含 503）→ `test_fault_probe.py` **11 passed**；冷卷复跑日志里可见 `== stack not usable yet (503); retrying in 2s` 后 `PASS: the crashed consumer's job was reclaimed and completed` |

## 备注

- 这不是"新功能引入的缺陷"，而是**守护网自身从未生效**：`gh run list --workflow nightly` 5 条全是 failure，说明 P2 之后没有任何一次自动回归真正跑过 deploy / fault 层。
- 冷卷复现在本机需要 `docker compose --profile smoke down -v`（删 `better-resume_pgdata` / `uploads`，均为历次演练残留，migrate 可重建）。