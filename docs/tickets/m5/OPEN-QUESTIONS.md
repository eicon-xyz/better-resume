# M5 待确认项（决策请拍板，我不自行决议）

| 编号 | 一句话 | 我的提议 |
| --- | --- | --- |
| Q1 | 星云凭据策略 | 完整实现 adapter + 本地假星云服务端跑契约测试，真机留 T10 等你给凭据 |
| Q2 | 绑定存哪 | DB 表 + 进程内缓存 + 端点（换供应商不发版）；环境变量只放密钥 |
| Q3 | 切换粒度 | **全局按场景**（一个场景一个绑定），不做按用户/按会话 |
| Q4 | 星云 adapter 的流式范围 | 同时支持 complete 与 stream（聊天场景要流式，其余四个走结构化输出） |
| Q5 | 前端范围 | `/settings/ai` 面板：查看 + 切换 + 未配置提示（不做密钥输入、不做用户级） |

---

## Q1 星云凭据

本机没有 `apiKey / apiSecret / flowId`。

- **A（提议）**：T3/T4 按 §4.1.8 的形状完整实现，契约测试用**本地假星云服务端**（CI 可跑）；
  T8 用假服务端做双供应商对照；T10 单列真机验证，等你的凭据。验收文档区分"契约级"与"真机"。
- B：只做接口与假实现，星云 HTTP/SSE 解析等有凭据再写（省 1.5 会话，但 M5 的"双 adapter"就只剩壳）。
- C：你现在就给凭据，我直接做真机（T10 与 T3 合并验收）。

## Q2 绑定存哪

- **A（提议）**：新表 `llm_scene_bindings`（scene 主键 + adapter + target_ref + updated_at），
  进程内缓存 + `GET/PUT /api/v1/scenes` 受保护端点；密钥仍然只从环境变量读。
  理由：这正是旧项目 `agent_properties` + 场景绑定三件的等价物，也是"换供应商不发版"的形态。
- B：只放 settings（环境变量）：`BR_SCENE_QUESTION_EXTRACTION=xingyun:flow-123`。
  省一张表和迁移，但改绑定要重启，且前端面板没法做。

## Q3 切换粒度

- **A（提议）**：全局按场景。理由：作品集范围内"一个场景一个绑定"已经能完整证明接缝，
  且测试/演示都简单；多租户是另一个维度的复杂度。
- B：按用户（每个用户自己的绑定）——需要用户表与管理语义，M5 会膨胀。
- C：按会话（每场面试可选供应商）——演示好看，但会让幂等/单飞 key 语义变复杂（同一会话两套结果）。

## Q4 流式范围

- **A（提议）**：两个都支持。聊天场景必须流式（M1 的打字机 + reasoning 面板要能复用），
  其余四个场景是结构化输出，走 `complete()`。
- B：先只做 `complete()`，聊天链路继续只走 openai_compat。
  这会让"四条链路全部可切换"的验收打折（聊天是四条之一）。

## Q5 前端范围

- **A（提议）**：新增 `/settings/ai`：五场景表格 + 切换 + "未配置（缺哪个变量）"提示 + 说明文案。
- B：只在 chat 页加一个只读徽标，切换靠 curl（省 1 会话，但"换供应商"这件事就没有界面证据）。

