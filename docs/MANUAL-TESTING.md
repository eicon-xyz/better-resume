# 人工测试清单（统一入口）

> 面向：你（唯一能点浏览器和麦克风的人）。
> 原则：**自动化已覆盖的部分不要重复手测**（见 §4）；这里只列"必须由人操作真设备/真浏览器"的事项。
> 发现问题：直接把现象（页面 + 复现步骤 + 截图）发给我，我来定位并红-绿修复。

## 0. 环境准备（每次手测前 1 分钟）

1. 栈在跑：`docker compose up -d --wait --scale api=2`，然后 `curl http://127.0.0.1:8080/healthz` 应为 200；
2. 浏览器 **Ctrl+Shift+R** 强刷（改过前端后必须，否则跑的还是旧 bundle）；
3. 地址：`http://127.0.0.1:8080`；登录门输入任意用户名（如 `demo`）→「进入」；
4. 建议用 Chrome（本项目只验过 Chrome；Safari/Firefox 见 §2 可选项）。

## 1. 必测：实时转写（P1-A 核心交付，本机已切 paraformer-rt）

**背景**：转写已从"松手后整段出字"（批量）切换为"**边说边出字**"（实时）。这是本次最重要的手测——
自动化只验到了 WS 协议层（穿 nginx 的增量时序），真实麦克风节奏只有你能验。

### 1.1 面试间答题框

| # | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入面试间（上传简历/示例进入），点击「语音输入」 | 按钮变为「停止录音」，无报错 |
| 2 | **说话过程中**观察答题框 | 文字**逐字/逐句出现**（不是等松手一次性出现） |
| 3 | 说话中途点击「停止录音」 | 已说的内容**完整保留**（不丢尾句） |
| 4 | 再录一段（连续说 2–3 句） | 新句**追加**在后面；旧内容不被改写/覆盖 |
| 5 | 录音期间**手动打字**（在框里敲几个字）→ 停止录音 | 手写内容**原样保留**，转写**追加**在其后（绝不覆盖） |
| 6 | 连续两轮后手动编辑过文字，再录第三轮 | 新转写**追加**；编辑过的内容不被吞掉 |

### 1.2 对话页（chat）同一通道

| # | 步骤 | 预期 |
| --- | --- | --- |
| 7 | 对话页点「语音输入」→ 说一句 → 停止 | 转写进输入框；发送后 AI 流式回复正常（SSE） |

### 1.3 TTS 题目播报

| # | 步骤 | 预期 |
| --- | --- | --- |
| 8 | 面试间点「播报题目」 | 语音播放正常；再次点击可停止；无 500/无假音频 |

### 1.4 观察项（顺手记一下，不用截图）

- 首字出现大约几秒（自动化实测 0.56s，浏览器真实麦克风可能略慢）；
- 说话中文字是"流畅跟手"还是"一顿一顿"；
- 停止录音到句子定稿大约几秒（自动化实测 ~1.1s）。

## 2. 可选：边界与矩阵（有余力再测）

| 项 | 怎么测 | 为什么值得 |
| --- | --- | --- |
| Safari / Firefox | 同 §1.1 跑一遍 1–5 | 浏览器矩阵从未验过（本项目只验 Chrome） |
| 蓝牙耳机麦克风 | 连蓝牙后重跑 1.1 的 1–4 | 采样率/设备切换是转写常见坑 |
| 长段连续说话（>30s 不停） | 一口气说 30s+ 再停 | 实时 ASR 长跑表现未验（最长只到 13.1s） |
| 断网恢复 | 录音中关 Wi-Fi 5s 再恢复 | 看前端错误态与恢复行为（预期：报错不糊弄，恢复可继续） |
| 第二台设备/无痕窗口 | 同时登录同一用户名 | 会话/互踢行为未定义过（可顺手观察） |

## 3. 等你输入才能测的（不是手测，是缺输入）

| 项 | 需要什么 |
| --- | --- |
| 星云等价物（百炼应用调用）端到端 | 你在百炼控制台**创建应用**后把 **app_id** 给我 |
| 讯飞 AST 真机 | 讯飞三件套（大概率不做，阿里已覆盖同类能力） |

## 4. 已自动化、无需手测的（避免重复劳动）

| 层 | 覆盖 | 触发 |
| --- | --- | --- |
| 单元/组件 | 后端 708 + 前端 161 | 每次 push/PR（CI） |
| 契约/静态 | ruff / eslint / tsc / alembic / 契约三件套 `--check` | 同上（经 `scripts/verify.sh`） |
| 部署面 | compose smoke / kill 实例演练 | **nightly**（每天 02:30 UTC）+ 手动 dispatch |
| 故障/浸泡 | 5 个故障实验 / 分区 / 主从切换 / 60 分钟浸泡 | nightly（快版）/ weekly（全量 + 长浸泡）+ 手动 |
| 真机 | 四链路 / 实时+批量 ASR / 句池回放 / 端到端 | **手动**：`bash scripts/verify.sh --layer real`（预算守卫，阶段收口必跑） |

一键入口：`bash scripts/verify.sh --layer all`（本机）。`--list` 可看全部层与命令。

## 5. 结果回填

测完把每项标 ✅/❌ + 一句话现象发我即可；❌ 项我按红-绿流程修复后你再复测。
全部 ✅ 后：P1 + P2 验收即完成，我开 PR（`p1/realtime-asr` → main）。

### 测试记录

| 轮次 | 日期 | 结果 |
| --- | --- | --- |
| 第 1 轮 | 2026-09-15 | §1 步骤 1–4 ✅；步骤 5 ❌（录音中手打字乱）→ **P28 已修**（span 替换语义，前端 168 全绿，nginx 已重建）；chat 回复偏慢 → 实测首 token 1.36s 在基线内（1.2–1.9s），服务端零异常，判供应商波动/冷启动 |
| 第 2 轮（待做） | — | 强刷后复测步骤 5（录音中打字应零交错）+ 感受 chat 速度 |
